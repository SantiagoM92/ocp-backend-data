apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend-data.configMapName" . }}
  labels:
    {{- include "backend-data.labels" . | nindent 4 }}
data:
  EXTERNAL_USERS_BASE_URL: {{ .Values.config.externalUsersBaseUrl | quote }}
